# @argus/sample-shop

Placeholder. The Next.js demo target app — a small login + product list + cart with stable
`data-testid`s — is built in milestone **M1 (TRE-30)**. It is the app Argus generates tests
against, and the one we deliberately "drift" to demo self-healing in **M3**.
